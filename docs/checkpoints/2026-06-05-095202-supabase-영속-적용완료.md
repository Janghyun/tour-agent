---
title: Supabase 영속 적용 완료 (CLI로 테이블 생성)
status: in-progress
branch: main
commit: dd01bf1
date: 2026-06-05T09:52:02+0900
files:
  - supabase/migrations/20260605000000_room_state.sql
  - src/tour_agent/main.py
  - src/tour_agent/supabase_store.py
---

## 하던 일
사용자 "supabase cli 설치돼 있으니 너가 할 수 있지 않아?" → 직접 테이블 생성·검증.

## 한 일
- supabase CLI 로그인 상태 확인 → tour 프로젝트(ref dyugshmubwmeirwtbzea, Tokyo) = SUPABASE_URL 일치.
- supabase link --project-ref ... --yes (DB password 없이 access token으로 link).
- 마이그레이션 생성(supabase/migrations/20260605000000_room_state.sql) → supabase db push 로 원격 적용.
  room_state 테이블 + RLS 정책(anon/authenticated 전체 허용, MVP) + grant.
- 검증: 백엔드 재시작 → [store] Supabase 영속 사용. WS로 방 저장 → Supabase DB 직접 조회로
  destination/owner/후보 유지 확인. 검증용 테스트 행 정리(204).

## 현재 동작(키 연동 모두 완료)
- 봇: 구독(cli) 경로. .env의 무효 ANTHROPIC 키는 cli 모드에서 무시.
- Kakao: 실검색·실동선·실좌표 지도 동작.
- Supabase: 방 상태 영속(재시작에도 유지).

## 남은 작업
1. [자격증명] 배포 — 진짜 ANTHROPIC_API_KEY(api 모드) + Fly/Cloudflare 토큰. 사용자 선택상 보류.
2. [선택/품질] 일정 서브에이전트 3단 연결, 외부 링크 후보 추가, tweaks 패널.

## 메모
- supabase/.temp·.branches는 gitignore(연결 정보). migrations만 커밋(재현용 = 다른 PC에서 supabase db push).
- RLS는 방 코드 공개 협업 MVP 수준. 실서비스 전 방 멤버십 검증 강화 권장.
- 떠 있는 서버: 백엔드 0.0.0.0:8000(BACKEND=cli, Kakao+Supabase 활성), 프론트 0.0.0.0:5173.
- 직전 체크포인트: Kakao 실검색 + Supabase 연동.
