---
title: 일정 고도화·종합검색·기록 영속·채팅 영속
status: in-progress
branch: main
commit: 1119835
date: 2026-06-05T18:58:36+0900
files:
  - src/tour_agent/itinerary_build.py
  - src/tour_agent/cards.py
  - src/tour_agent/messages.py
  - src/tour_agent/naver.py
  - src/tour_agent/google_places.py
  - src/tour_agent/search_aggregate.py
  - web/src/cards.jsx
  - web/src/mapmodal.jsx
---

## 이번 세션 완료(키 없이 가능한 것)
- 일정 백엔드 이관(compose_itinerary): 봇은 장소 목록만 → 코드가 좌표(Kakao)·동선(order_route)·
  이동시간(directions)·시간표 조립. 일정 140초→60초. 충청도 이상치 제거, 좌표·이동시간 실데이터.
- 첫날 숙소=동선 마지막(체크인), 둘째날부터 숙소 출발. 미니맵/확대지도 숙소 핀(🏠).
- 맛집 대안 2~3곳(식사 항목 펼침). 식사 맛집 구체화.
- 실제 지도 확대 모달(leaflet+OSM, lazy). 미니맵 날짜별 분리, 번호 핀 클릭 정보.
- 봇 응답 경과 시간 + 완료 후 'N초 걸림' 유지. 인디케이터에 질문 표시.
- 요일 정확(오늘 날짜 KST 주입). 모델 cli=sonnet 고정.
- 채팅 메시지 백엔드 영속(room_message) — 새로고침·방 멤버·기기 공유.
- 내보낸 일정 기록 백엔드 영속(room_export) — 방 멤버 공유. HTML 내보내기/인쇄(PDF).
- 네이버·구글 종합 검색 구조(naver/google_places/search_aggregate) — 키 주입 시 자동 합산(현재 Kakao만).
- /후보 즉시 등록, 채팅 링크 후보 등록, 장소 클릭 카카오맵.

## 남은 것
- [키] 3) API 키로 속도 — 진짜 ANTHROPIC_API_KEY .env에 넣으면 api 모드(60초→~25초). 현재 cli(sonnet).
- [키] 네이버/구글 키 넣으면 종합 검색 활성. Kakao 외 소스.

## 검증
- 117 passed + cli e2e skip. 빌드·SSR 그린. Supabase 마이그레이션(room_state/message/export) 적용.
- 실 e2e: compose 일정(좌표·이동·대안), 첫날 체크인 마지막, 채팅/기록 방 공유, Kakao 실검색.
- 떠 있는 서버: 백엔드 cli 0.0.0.0:8000, 프론트 5173. 실방 S2E53E/2P92ZA(Supabase).

## 메모
- 속도 근본은 cli(구독) 봇 호출 오버헤드. api 모드가 해결. [[cli-subscription-e2e]]
