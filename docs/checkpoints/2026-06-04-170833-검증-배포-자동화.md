---
title: 검증·배포 자동화 (SSR 검증 + GitHub Actions)
status: in-progress
branch: main
commit: 0f2ca79
date: 2026-06-04T17:08:33+0900
files:
  - web/ssr-check.jsx
  - web/src/App.jsx
  - web/package.json
  - web/.gitignore
  - requirements-dev.txt
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
  - README.md
---

## 하던 일
Stop 훅이 짚은 미완 항목 중 **내 권한 안에서 가능한 것**을 진행:
(2) 브라우저 없는 시각 렌더 확인 → SSR 렌더 검증, (3) 실제 배포 → GitHub Actions 자동화.
실키 e2e와 실제 배포는 자격증명/계정이 필요해 여전히 사용자 몫.

## 내린 결정
- 브라우저 렌더 대신 `react-dom/server`로 App을 정적 렌더(`web/ssr-check.jsx`).
  `npm run ssr-check`로 재현 — 2807자 렌더, 마커("여행봇","봇 부르기") OK, exit 0.
  useEffect/WS는 SSR에서 안 돌지만 트리 구성·렌더 오류는 잡힌다.
- App.jsx의 `location` 접근을 `_loc` 가드로 SSR-safe 처리(렌더가 죽지 않게).
- CI(`ci.yml`): 푸시·PR마다 백엔드 72테스트 + 프론트 빌드 + SSR 검증. 시크릿 불필요.
- 배포(`deploy.yml`): main 푸시·수동. Fly(백엔드)·Cloudflare Pages(프론트).
  각 잡은 해당 시크릿 있을 때만 동작(guard step → `if`), 없으면 깔끔히 건너뜀.
  런타임 키는 레포·CI가 아니라 `fly secrets set`으로 별도 주입.

## 남은 작업 (환경/자격증명 또는 점진 폴리시)
1. [자격증명] 실키 e2e — Anthropic(봇)·Kakao(검색/동선)·Supabase. 사용자 키 필요.
2. [자격증명] 실제 배포 — Fly 토큰 + Cloudflare 토큰/계정ID를 GitHub Secrets에 넣고 main 푸시.
3. [폴리시] 룸 셸/사이드 패널/로비/지도 실연동(Kakao Map) 완전 파리티, 모바일 바텀시트.

## 메모
- 검증 수준(여기 한도): 백엔드 72테스트 + docker 기동 + ws.js node e2e + vite build + SSR 렌더.
- 모든 신규/수정 파일 한자·깨짐 검사 클린. .env 미추적 확인.
- 직전 체크포인트: 2026-06-04-165835-프론트-채팅-컴포저-파리티.
- 푸시 완료: 8dc4fac..0f2ca79.
