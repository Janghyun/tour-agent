---
title: Kakao 실검색 + Supabase 영속 연동
status: in-progress
branch: main
commit: 01aa7ca
date: 2026-06-05T09:44:26+0900
files:
  - src/tour_agent/main.py
  - src/tour_agent/supabase_store.py
  - docs/supabase_schema.sql
---

## 하던 일
사용자가 .env에 키 제공 → 확인하고 연동. KAKAO 유효(실검색 동작), SUPABASE 키 있음(연결),
ANTHROPIC은 10자라 무효(사용자: 로컬은 cli 구독으로만, 배포는 추후 진짜 키).

## 한 일 / 검증
- .env 자동 로드(python-dotenv). KAKAO_REST_API_KEY로 실검색·실동선 활성.
  - 버그 수정: cli(구독) 모드에서 .env의 가짜 ANTHROPIC_API_KEY가 SDK API 인증을 유발해
    'Invalid API key' → cli 모드에선 환경에서 제거. 이후 Kakao 실검색 e2e OK(실좌표 5/5),
    실좌표 후보로 order_route 일정 OK.
- Supabase 영속: SupabaseRowStore(httpx PostgREST 어댑터, MockTransport 테스트 3종),
  main이 SUPABASE 키 있으면 시작 헬스체크 → 성공 시 SupabaseStateStore, 실패 시 인메모리 fallback.
  실연결 진단: 키 인증 OK, 테이블(room_state) 미생성으로 현재 fallback 동작 확인.
- 85 passed, CI 통과 예정.

## 남은 작업
1. [사용자] Supabase 테이블 생성 — docs/supabase_schema.sql 을 대시보드 SQL Editor에서 실행(1회).
   이후 백엔드 재시작하면 [store] Supabase 영속 사용으로 전환. 재시작에도 방 상태 유지.
2. [자격증명] 배포 — 진짜 ANTHROPIC_API_KEY(api 모드) + Fly/Cloudflare 토큰.
3. [선택/품질] 일정 서브에이전트 3단 연결, 외부 링크 후보 추가(키), tweaks 패널.

## 메모
- 떠 있는 서버: 백엔드 0.0.0.0:8000(BACKEND=cli, .env 로드 → Kakao 활성, 현재 store=인메모리 fallback).
- SUPABASE_KEY는 publishable(anon)이라 RLS 정책 필요 — schema.sql에 포함.
- 직전 체크포인트: 남은 기능 개발완료. 관련 [[cli-subscription-e2e]].
