---
title: 남은 기능 개발완료 (패널 탭·선호·비교·지도·방전환·모바일·일정 후보반영)
status: in-progress
branch: main
commit: a8ff311
date: 2026-06-05T08:04:00+0900
files:
  - src/tour_agent/state.py
  - src/tour_agent/routing.py
  - src/tour_agent/cards.py
  - src/tour_agent/prompts.py
  - web/src/panel.jsx
  - web/src/map.jsx
  - web/src/cards.jsx
  - web/src/App.jsx
---

## 하던 일
/goal "남은 항목 이슈 없이 개발완료". 키/계정 불필요한 남은 기능을 디자인 원본대로 구현하고 종단 검증.

## 완료(구현+검증)
- 선호 토글: state.set_preference(여행자별 토글) + actions + 후보 탭 좋아요/싫어요. WS 종단 OK.
- 사이드 패널 탭: 후보/일정/지도(panel.jsx). 지도는 실좌표 정규화 핀(map.jsx, 제주 일러스트 대신 범용).
- 비교 카드: cards.py present_compare + COMPARE_SCHEMA, web CompareCard, 프롬프트 안내. 구독 e2e OK.
- 방 전환기: 방 헤더 드롭다운(내 방 목록 전환·로비).
- 모바일: <=760px 패널 토글 오버레이.
- 검색: 프롬프트 보강으로 present_place_options 자동(키 없으면 봇 지식). 구독 e2e OK.
- 일정 후보 반영 버그 수정: 작업 경로에 방 상태 스냅샷(후보 풀 포함) 주입. 재검증 담은 후보 3/3.

## 검증 수준
82 passed(+1 skip cli e2e), vite build 그린, SSR OK, dev 모듈 변환 200,
구독 봇 e2e: 검색/비교/일정 카드 OK, WS 종단 선호 토글·방생성 메타 OK, CI 통과.

## 남은 항목(권한 밖 또는 제품 결정 — 의도적 보류)
1. [자격증명] Kakao 실검색·실동선 — KAKAO_REST_API_KEY. 실좌표 들어오면 지도 핀·order_route 동선 실작동.
2. [자격증명] 실제 배포 — Fly+Cloudflare 토큰(배포는 약관상 BACKEND=api → ANTHROPIC_API_KEY).
3. [범위밖/키] 외부 링크로 후보 추가(LinkAdd) — 네이버/카카오 URL 파싱에 키·스크래핑 필요.
4. [범위밖/품질] 일정 미세조정 패널(tweaks) — working_itinerary가 이름 목록이라 슬롯/시간 편집 불가.
   봇 자연어 수정("@봇 점심을 X로 바꿔")으로 대체하는 게 제품 방향.
5. [품질] itinerary.py 설계->동선->검증 3단 오케스트레이션 런타임 연결 — 현재 단일 봇이 일정 정상 생성.
   연결 시 ORCHESTRATOR 흐름 대수술이라 리스크, 추후 품질 고도화로.

## 메모
- 떠 있는 서버: 백엔드 0.0.0.0:8000(BACKEND=cli), 프론트 0.0.0.0:5173.
- 관련 메모리 [[cli-subscription-e2e]]. 직전 체크포인트: 로비 진입 화면.
