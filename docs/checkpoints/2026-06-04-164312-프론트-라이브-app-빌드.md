---
title: 프론트 라이브 App 빌드
status: in-progress
branch: main
commit: d70ca05
date: 2026-06-04T16:43:12+0900
files:
  - web/src/App.jsx
  - web/src/icons.jsx
  - web/src/ws.js
  - web/index.html
  - web/package.json
  - web/vite.config.js
  - src/tour_agent/app.py
  - tests/test_app.py
---

## 하던 일
Vite+React 라이브 앱 구축. 백엔드 WS에 연결해 채팅·present_* 카드·상태를 렌더하고
후보 추가/확정 액션을 보낸다. 디자인 토큰(styles.css)으로 입힘. `vite build` green.
포팅 중 백엔드 갭(사람 채팅 미브로드캐스트)을 발견·수정. **백엔드 72 테스트 통과.**

## 내린 결정
- 프론트는 백엔드 데이터로 구동(데모 Scenario/classifyIntent/고정지도 제거). 현재 라이브 핵심 흐름 동작.
- 사람 채팅도 방 전체에 브로드캐스트(보낸 사람 포함, 에코로 렌더) — 그룹챗 필수.
- 픽셀 파리티(로비·옵션비교·지도 실연동·모바일 시트·에러/로딩 풀세트)는 점진 폴리시.

## 남은 작업
1. **라이브 UI 브라우저 검증** — backend(uvicorn:8000) + `npm run dev` 띄우고 browse로 연결→채팅 에코 렌더 확인(키 없이 가능). 봇 카드→추가→상태 흐름은 Anthropic 키 필요.
2. 디자인 전체 화면 픽셀 파리티 포팅(cards/panel/chat/lobby/map 컴포넌트), 모바일 반응형.
3. 실키 e2e(Anthropic/Kakao/Supabase), 배포(Fly 백엔드 + Cloudflare Pages 프론트).

## 메모
- 검증 수준: 백엔드 72테스트 + docker 이미지 기동 + ws.js node e2e + vite build green. 미검증=라이브 React 렌더(브라우저)·실키 봇 e2e.
- 프론트 WS 주소 `VITE_WS_BASE`(기본 ws://host:8000), 방·이름 `?room=&me=`.
- 직전 체크포인트: 2026-06-04-162752-프론트-ws-클라이언트-검증.
