---
title: 카드 디자인 파리티
status: in-progress
branch: main
commit: 673efeb
date: 2026-06-04T16:52:46+0900
files:
  - web/src/cards.jsx
  - web/src/constants.js
  - web/src/App.jsx
---

## 하던 일
프론트 픽셀 파리티 진행 — 디자인 카드 컴포넌트(PlaceOptions/Itinerary/Map + Thumb/CatPill)를
styles.css 디자인 클래스로 포팅하고, 백엔드 present_* 페이로드로 구동. 카테고리는 catKey로
Kakao 자유 텍스트 → 디자인 분류 정규화. vite build green(35 모듈).

## 내린 결정
- 디자인 카드 마크업/클래스를 그대로 쓰되 데이터는 백엔드 페이로드(인라인 장소)로. demo place(id) 의존 제거.
- 카테고리 불일치(백엔드 "음식점 > 한식" vs 디자인 CAT) → catKey 정규화로 해소.

## 남은 작업
1. 픽셀 파리티 잔여 — 로비/룸 셸/패널/채팅 버블/지도 실연동·모바일 바텀시트(현재 룸 레이아웃은 디자인 토큰 기반 자체 구성).
2. 라이브 React 브라우저 렌더 확인(샌드박스 제약 — 사용자 환경 `npm run dev`).
3. 실키 e2e(Anthropic/Kakao/Supabase), 실제 배포(Fly + Cloudflare Pages).

## 메모
- 검증: 백엔드 72테스트 + docker 기동 + ws.js node e2e + vite build green. 카드는 디자인 클래스 사용.
- 환경 제약(여기서 불가): 브라우저 시각 렌더(exit 144), 실키 봇 e2e.
- 직전 체크포인트: 2026-06-04-164312-프론트-라이브-app-빌드.
