---
title: 배포 스캐폴딩 검증
status: in-progress
branch: main
commit: 139580b
date: 2026-06-04T16:21:01+0900
files:
  - Dockerfile
  - .dockerignore
  - fly.toml
  - requirements.txt
  - .env.example
---

## 하던 일
백엔드 배포 스캐폴딩 추가·검증. API 모드(Messages API) 단일 프로세스 이미지.
`docker build` 성공 + 컨테이너 기동 확인(`Application startup complete`, GET / → 404 = 서버 정상).

## 내린 결정
- 이미지는 **python:3.12-slim, node 없음**(CLI/SDK는 로컬 전용). `PYTHONPATH=/app/src`, `uvicorn tour_agent.main:app:8080`.
- Fly: 방별 **인메모리 세션** 때문에 `min_machines_running=1`(스케일 시 room 단위 sticky 필요).
- 프론트(`web/`)는 **별도 배포(Cloudflare Pages)** — `.dockerignore`로 백엔드 이미지에서 제외.
- 시크릿은 `fly secrets`/환경변수로만(레포 금지). `.env`는 gitignore, `.env.example` 제공.

## 남은 작업
1. **프론트↔백엔드 실연결** — `web/` 데모 인텔리전스 제거, WS로 present_*/state 소비, 액션 호출, 손그림 지도 → Kakao Map.
2. **실키 런타임 검증(자격증명 필요)** — Anthropic Messages API, Kakao REST, Supabase RowStore 어댑터.
3. 에러/로딩 풀세트, 확정본↔작업본 뷰.

## 메모
- 검증 경계: 여기 환경엔 Anthropic·Kakao·Supabase 키가 없어 *실호출 e2e*는 불가. 로직·이미지 기동은 검증됨(테스트 71 + docker build/boot).
- 로컬 이미지 태그 `tour-agent:test` 빌드됨(레포 외부). 배포: `fly launch`/`fly deploy` + `fly secrets set ...`.
- 직전 체크포인트: 2026-06-04-161703-백엔드-기능-완성.
