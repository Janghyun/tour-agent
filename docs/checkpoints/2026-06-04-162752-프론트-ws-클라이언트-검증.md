---
title: 프론트 WS 클라이언트 검증
status: in-progress
branch: main
commit: cf40deb
date: 2026-06-04T16:27:52+0900
files:
  - web/ws.js
---

## 하던 일
프론트↔백엔드 WebSocket 통합의 핵심인 **프로토콜 클라이언트 `web/ws.js`** 작성·검증.
`connectRoom(url,{onText,onCard,onState,onError}) → sendChat/sendAction`. UMD(브라우저·node 양용).
실서버(uvicorn `main:app`, 키 없이)에 node로 e2e: `add_candidate` 액션 → `state` 수신 확인.

## 내린 결정
- WS 클라이언트는 프레임워크 무관(UMD) — 프로토타입 패치든 Vite 재작성이든 그대로 재사용.
- 검증 경계: **액션 경로는 키 없이 검증 가능**(node e2e 통과). **봇 응답 카드 e2e는 Anthropic 키 필요**.

## 남은 작업 (프론트 라이브 앱 — 방식 결정 필요)
1. `web/app.jsx`(데모, 400줄)의 `classifyIntent`/`Scenario`/역할토글/데모시계 제거하고 ws.js로 라이브화.
   - HANDOFF 권고는 "프로토타입 패치가 아니라 프레임워크 재작성".
   - 선택: (a) Vite+React 재작성, (b) 프로토타입에 라이브 모드 패치, (c) Anthropic 키로 봇 e2e까지 검증.
2. 백엔드 카드 페이로드(place_options/itinerary/map) ↔ 프론트 카드 컴포넌트 shape 매핑.
3. 브라우저 검증(browse): 연결→add_candidate→상태 패널 갱신(키 없이), 봇 카드(키 필요).

## 메모
- 백엔드는 배포 가능 + 71 테스트 + 이미지 빌드/기동 검증 완료. ws.js로 프론트 브리지 검증.
- 실키(Anthropic/Kakao/Supabase) e2e는 자격증명 대기.
- 직전 체크포인트: 2026-06-04-162101-배포-스캐폴딩-검증.
